[flake8]
max-line-length = 88
extend-ignore = E203, W503
exclude =
    .git,
    __pycache__,
    .venv,
    venv,
    build,
    dist

[isort]
profile = black
multi_line_output = 3

[tool:pytest]
asyncio_mode = auto
testpaths = tests
